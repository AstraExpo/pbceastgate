fragment MinistryFields on Ministry {
  id
  name
  description
  headId
}

query GetMinistries {
  ministries {
    ...MinistryFields
  }
}

query GetMinistry($id: String!) {
  ministry(id: $id) {
    ...MinistryFields
  }
}
