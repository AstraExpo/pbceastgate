fragment UserFields on User {
  id
  firebaseUid
  name
  email
  image
  emailVerified
  banned
  banReason
  banExpires
  systemRole
  membershipStatus
  createdAt
  updatedAt
}

mutation AuthenticateAdmin {
  authenticateAdmin {
    ...UserFields
  }
}
